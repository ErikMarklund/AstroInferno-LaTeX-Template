\documentclass[letterpaper,twocolumn,openany,nodeprecatedcode]{astroinfernobook}

\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage[singlelinecheck=false]{caption}
\usepackage{lipsum}
\usepackage{listings}
\usepackage{shortvrb}
\usepackage{stfloats}
\usepackage[normalem]{ulem} % For strikethrough

\captionsetup[table]{labelformat=empty,font={sf,sc,bf,},skip=0pt}

\MakeShortVerb{|}

\lstset{%
  basicstyle=\ttfamily,
  language=[LaTeX]{TeX},
  breaklines=true,
}

\newcommand{\so}[1]{\sout{\color{gray}{#1}}}

\begin{document}

\onecolumn{
  {\hfill\Huge Backlog \hfill\null}
  \vspace{2cm}
}
\section{General}
\begin{itemize}
\item[x] \so{Make own README.md}
\item Make new scrot.jpg preview
\item[x] \so{Rework CONTRIBUTING.md}
\item[x] \so{Flush CHANGELOG.md}
\item Some specifics:
  \begin{itemize}
  \item[x] \so{Point to Astro Inferno Third Party License}
  \item[x] \so{Incorporate the exact licence text}
  \item[x] \so{Incorporate Astro Inferno logo}
  \item[x] \so{Incorporate Astro Inferno Coven logo}
  \item[x] \so{Mention font licenses}
  \item[x] \so{Specify that this is made for LuaLaTeX, not tested with pdfLaTeX}
  \end{itemize}
\end{itemize}

\section{Cleanup}
\begin{itemize}
\item Remove all DnD references and examples of DnD specific functionality
\item Remove redundant functions
  \begin{itemize}
  \item[x] \so{Spells}
  \item[x] \so{Redundant Monster code}
  \item Various colours
  \end{itemize}
\end{itemize}

\section{Astro Inferno features}
\subsection{Cosmetic}
\begin{itemize}
\item[x] \so{Load font for quotes/verses. Should resemble 1651 Alchemy.}
\item[x] \so{Page numbers in Avalen Rekas, placed in corners}
\item[x] \so{Improve location of page numbers. Closer to page corner.}
\item Define new colours
\item Provide some more background images for light and dark pages
\item[x] \so{Remove footer line}
\item[x] \so{Remove chapter name from footer}
\item[x] \so{Choose white logo if dark mode}
\end{itemize}

\subsection{Functionality}
\begin{itemize}
\item Replace |AISetThemeColor[AIdarkbox]| with separate calls to set different colours for different elements.
\item[x] \so{Make function for detecting dark mode}
\item[x] \so{Implement function for inserting new background image without
  switching mode.}
\item Fixes:
  \begin{itemize}
  \item[x] \so{Positioning of the page numbers is wrong when switching back
    to light mode.}
  \end{itemize}
\end{itemize}

\subsection{Examples}
\begin{itemize}
\item[x] \so{|AIMakeDark| and |AIMakeLight|, and |AIIsDarkMode|}
\item[x] \so{Symbols and macros for calling them (|AIdamage|, \ldots)}
\end{itemize}
\end{document}
