\documentclass[letterpaper,twocolumn,openany,nodeprecatedcode]{astroinfernobook}

\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage[singlelinecheck=false]{caption}
\usepackage{lipsum}
\usepackage{listings}
\usepackage{shortvrb}
\usepackage{stfloats}
\usepackage[normalem]{ulem} % For strikethrough

\captionsetup[table]{labelformat=empty,font={sf,sc,bf,},skip=0pt}

\MakeShortVerb{|}

\lstset{%
  basicstyle=\ttfamily,
  language=[LaTeX]{TeX},
  breaklines=true,
}

\definecolor {fixnotecolour} {rgb} {0.3, 0.6, 0.5}
\newcommand{\so}[1]{\sout{\color{gray}{#1}}}
\newcommand{\fixnote}[1]{\textit{\color{fixnotecolour} {#1}}}
\begin{document}

\onecolumn{
  {\hfill\Huge Backlog \hfill\null}
  \vspace{2cm}
}
\section{General}
\begin{itemize}
\item[x] \so{Mark which fonts are for personal use in astroinfernofonts.sty}\\
  \fixnote{Added comment.}  
\item[x] \so{Make own README.md}
\item[x] \so{Make new scrot.jpg preview}
\item[x] \so{Rework CONTRIBUTING.md}
\item[x] \so{Flush CHANGELOG.md}
\item Some specifics:
  \begin{itemize}
  \item[x] \so{Point to Astro Inferno Third Party License}
  \item[x] \so{Incorporate the exact licence text}
  \item[x] \so{Incorporate Astro Inferno logo}
  \item[x] \so{Incorporate Astro Inferno Coven logo}
  \item[x] \so{Mention font licenses}
  \item[x] \so{Specify that this is made for LuaLaTeX, not tested with pdfLaTeX}
  \end{itemize}
\end{itemize}

\section{Cleanup}
\begin{itemize}
\item Harmonise colour names
\item[x] \so{Organise fonts into folders}
\item[x] \so{Clean up keys \{ astroinferno / fonts \}. Style commands should
  be in *-style, not *-family}
\item Remove all DnD references and examples of DnD specific functionality
\item Remove redundant functions
  \begin{itemize}
  \item[x] \so{Superfluous header code}
  \item[x] \so{Spells}
  \item[x] \so{Redundant Monster code}
  \item[x] \so{Various colours}
  \item[x] \so{Some fonts}
    \begin{itemize}
    \item[x] \so{gillius2}
    \item[x] \so{jkpss}
    \end{itemize}
  \end{itemize}
\end{itemize}

\section{Astro Inferno features}
\subsection{Cosmetic}
\begin{itemize}
\item[x] \so{Parts are a bit \ldots dull.}\\
  \fixnote{Made 'Part X' small and floating above the first letter. Part name larger.}
\item[x] \so{Consistent British or American English}\\
  \fixnote{British English now.}
\item[x] \so{Bring in left margin of full-page |AIWeapon|}
\item[x] \so{Semitransparent weapon boxes}
\item[x] \so{Semitransparent boxes?}\\
  \fixnote{Still opaque by default.}
\item[x] \so{Readaloud boxes do not look very Astro Inferno}\\
  \fixnote{More plain now, which is better.}
\item[x] \so{Subparagraphs look just like paragraphs now, so remove them}\\
  \fixnote{Removed from example.tex, but they can in principle
    be used by AISubArea under some circumstances, so they are still
    defined. subiparagraph is gone however.}
\item[x] \so{Allow image border}
\item[x] \so{Make coloured border}
\item[x] \so{Print chapter etc vertically in margin}
\item[x] \so{Make macro for titlepage}
\item[x] \so{Make macro for TOCpage}
\item[x] \so{Make macro for imagepage (last page in example.tex)}
\item[x] \so{Find block font for chapter ``exponents''}\\
  \fixnote{Bahnschrift, fallback to D-DIN.}
\item[x] \so{Change TOC font(s)}
\item[x] \so{Swap out Eroded for Cheddar Gothic}
  \begin{itemize}
  \item[x] \so{Delete Cheddar Gothic from fonts due to licensing. Still load if available,
      but use Eroded as backup.}\\
    \fixnote{Added Eroded for personal use and fallback mechanics.}
  \end{itemize}
\item[x] \so{Center text vertically in sidebars}
\item[x] \so{Use gap between paragraphs instead of indents}
\item[x] \so{Enlarge (sub)section/chapter headings}
\item[x] \so{Remove 'Chapter X:' from chapter headings or make optional}
\item[x] \so{AIWeapon dark/light mode sensitive}
\item[x] \so{Comment, readaloud, sidebar fonts}
\item[x] \so{Add Dominican as additional fallback for 1651 Alchemy}
\item[x] \so{Good font for dropcap? (Not used in AI so not hugely important)}
\item[x] \so{Find good 'Old English' font.}
\item[x] \so{Load font for quotes/verses. Should resemble 1651 Alchemy.}
\item[x] \so{Page numbers in Avalen Rekas, placed in corners}
\item[x] \so{Improve location of page numbers. Closer to page corner.}
\item[x] \so{Make page number location more robust.
  Only single-digit numbers correct. Needs to be properly left/right adjusted.}
\item[x] \so{Define new colours}
\item Provide some more background images for light and dark pages
\item[x] \so{Remove footer line}
\item[x] \so{Remove chapter name from footer}
\item[x] \so{Choose white logo if dark mode}
\item[x] \so{Change table font}
\end{itemize}

\subsection{Functionality}
\begin{itemize}
\item[x] \so{Master environment}
\item[x] \so{AIMonster does not need a tcolorbox}\\
  \fixnote{Now a simple minipage.}
\item Better control over mode-specific colours
\item[x] \so{Weapon images}\\
  \fixnote{Pretty dirty. Could do with cleaner implementation.}
\item[x] \so{Modular frames/looks for comment, readaloud, sidebar}
\item[x] \so{Options for more spatious chapter headings}\\
  \fixnote{Effectively added before-code and after-code.}
\item[x] \so{Automate turning off border for AIMakeElementImagepage}
\item[x] \so{Make border printing optional}
\item[x] \so{Simplify nested logic in |fancyfoot|}
\item[x] \so{Allow AIMonster to have preset height for adversary name}
\item[x] \so{Make |AIMonster| unbreakable by default}
\item[x] \so{Make a macro for the Astro Inferno copyright text}
\item[x] \so{Implement fallback system for fonts.}
\item[x] \so{Replace |AISetThemeColor[AIdarkbox]| with separate calls to set different colours for different elements.}
\item[x] \so{Make function for detecting dark mode}
\item[x] \so{Implement function for inserting new background image without
  switching mode.}
\item[x] \so{Make proper Astro Inferno weapon environment}
\end{itemize}

\subsubsection{Experiment}
\begin{itemize}
\item multicol instead of twocolumn
\item[x] \so{Trouble and endtrouble boxes should actually overlap to
    accommodate longer lines.}\\
  \fixnote{Works!}
\item Construct modular fallback font mechanics.
\end{itemize}

\subsection{Examples}
\begin{itemize}
\item[x] \so{Example for |AIMakeElementImagepage|}
\item[x] \so{|AIMakeDark| and |AIMakeLight|, and |AIIsDarkMode|}
\item[x] \so{Symbols and macros for calling them (|AIdamage|, \ldots)}
\item[x] \so{Loading and using fonts}
\item[x] \so{Explain Dominican font}
\end{itemize}

\subsection{Issues}
\begin{itemize}
\item Spacing after chapter title erratic with |\AINextChapterAfter|.
\item[x] \so{Border top about a millimeter too low}\\
  \fixnote{Made them 0.2~mm taller. Unclear why they were too short.}
\item[x] \so{Border text and page numbers follow mode, which can look bad on}
  border. Rulebook uses white whenever there is a border.
\item[x] \so{Allow commas in troubles in |AIMonster|}
\item[x] \so{modeafter option for |AIMakeElementImagepage| breaks
    compilation. Why?}\\
  \fixnote{Reimplemented with before-code and after-code, removed mode,
    modeafter etc. More robust.}
\item Investigate why readaloud-style doesn't automatically change to
  current mode colour with AIquoteFont. Same for sidebar.
\item[x] \so{AIloot needs fixing. AIloot\{+1\} makes double +}\\
  \fixnote{(didn't really need fixing. Was called wrong in example.tex)}
\item[x] \so{Positioning of the page numbers is wrong when switching back
    to light mode.}
\end{itemize}
\end{document}
